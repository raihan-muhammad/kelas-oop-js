const array = ['Hallo', 'nama', 'saya', [1, 2, 3]];

console.log(array[3][2]);

// array.map((data, index, array) => console.log(data, index, array));
// array.filter((data, index, array) => {
//   // console.log(data === 'Hallo' ? 'Sukses' : 'Gagal');
// });
